# netlify.toml
[build]
  command = "pnpm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
